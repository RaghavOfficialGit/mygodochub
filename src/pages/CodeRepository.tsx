const CodeRepository = () => (
  <div className="h-full w-full">
    <iframe
      src="https://yodacodepage.lovable.app"
      className="w-full h-full border-0"
      title="Code Repository"
      style={{ minHeight: 'calc(100vh - 3.5rem)' }}
    />
  </div>
);

export default CodeRepository;
